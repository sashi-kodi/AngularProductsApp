import { CloseCarouselDirective } from './close-carousel.directive';

describe('CloseCarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
